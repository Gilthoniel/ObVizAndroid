<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="colorPrimary">@color/ColorPrimary</item>
        <item name="colorPrimaryDark">@color/ColorPrimaryDark</item>
        <item name="android:textColorPrimary">#ffffffff</item>

    </style>

    <!-- Theme for main application -->
    <style name="MainAppTheme" parent="AppTheme">
        <item name="colorAccent">@color/appColor</item>
    </style>

    <!-- Theme for the compared app -->
    <style name="ComparedAppTheme" parent="AppTheme">
        <item name="colorAccent">@color/comparisonColor</item>
    </style>

    <!-- Theme for the Tutorial -->
    <style name="CustomTutorialTheme" parent="ShowcaseView">
        <item name="sv_backgroundColor">#a60e1e3e</item>
        <item name="sv_showcaseColor">@color/ColorPrimaryDark</item>
        <item name="sv_buttonText">Got it!</item>
        <item name="sv_tintButtonColor">true</item>
        <item name="sv_titleTextAppearance">@style/CustomTitle</item>
    </style>

    <style name="CustomTitle" parent="TextAppearance.ShowcaseView.Title">
        <item name="android:textColor">#ffffffff</item>
    </style>

</resources>
